```jsx
<FilterBar options={['dev','art','photography']} value={filter} onChange={setFilter} />
```

Only rendered when there is more than one option (photos/ hides it for a single year). Selected state = accent text + accent border + 3px accent hard shadow.
