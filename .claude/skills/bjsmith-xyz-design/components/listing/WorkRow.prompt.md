Work listings show the **slug**, not the title — the page is a file browser.

```jsx
<WorkRow slug="mspaint" category="art" date="2019-01-31" description="paint.exe" />
```

Columns: `5rem 6.5rem 1fr auto 1.5rem` (`--work-cols`). Description is ellipsised and hidden under 768px.
